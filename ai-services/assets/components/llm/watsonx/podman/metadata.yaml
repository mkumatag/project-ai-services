name: llm
version: "1.0.0"

podTemplateExecutions:
  - [watsonx-secret.yaml.tmpl]
  - [watsonx-server.yaml.tmpl]

# Resource requirements
resources:
  cpu: 2
  memory: 4294967296 # 4Gi in bytes
  storage: 0 # No persistent storage required (cloud-based service)


# Made with Bob
