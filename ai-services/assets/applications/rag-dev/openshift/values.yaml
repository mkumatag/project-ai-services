ui:
  # @hidden
  image: icr.io/ai-services-cicd/rag-ui:v0.0.23

backend:
  # @hidden
  image: icr.io/ai-services-cicd/rag:v0.0.67
  # @hidden
  log_level: "INFO"

summarize:
  # @hidden
  image: icr.io/ai-services-cicd/rag:v0.0.67
  # @hidden
  log_level: "INFO"

digitize:
  # @hidden
  image: icr.io/ai-services-cicd/rag:v0.0.67
  # @hidden
  log_level: "INFO"

digitizeUi:
  # @hidden
  image: icr.io/ai-services-cicd/digitize-ui:v0.0.7

instruct:
  # @hidden
  storageUri: hf://ibm-granite/granite-3.3-8b-instruct

embedding:
  # @hidden
  storageUri: hf://ibm-granite/granite-embedding-278m-multilingual

reranker:
  # @hidden
  storageUri: hf://BAAI/bge-reranker-v2-m3


ingest:
  # @hidden
  log_level: "INFO"

opensearch:
  # @hidden
  image: icr.io/ppc64le-oss/opensearch-ppc64le:3.3.0
  # @description Sets the memory limit for the Opensearch service(Default: 4Gi). Override by passing a value with a unit suffix (e.g., Mi, Gi).
  # @validator pattern: ^[0-9]+(\.[0-9]+)?(Mi|Gi|Ti)$
  # @validator required: true
  memoryLimit: 4Gi
  # @description Sets the storage limit for the Opensearch service(Default: 10Gi). Override by passing a value with a unit suffix (e.g., Mi, Gi).
  # @validator pattern: ^[0-9]+(\.[0-9]+)?(Mi|Gi|Ti)$
  # @validator required: true
  storage: 10Gi
  auth:
    # @validator required: true
    # @validator minLength: 3
    username: "admin"
    # @validator required: true
    # @validator minLength: 8
    password: "AiServices@1234"
