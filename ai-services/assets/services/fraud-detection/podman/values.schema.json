{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "Object storage",
  "additionalProperties": false,
  "properties": {
    "s3Endpoint": {
      "type": "string",
      "title": "S3 endpoint URL",
      "description": "Base URL of the S3-compatible object store. Example: https://s3.us-south.cloud-object-storage.appdomain.cloud"
    },
    "s3Bucket": {
      "type": "string",
      "title": "Bucket name",
      "description": "Name of the bucket that holds the model."
    },
    "s3Key": {
      "type": "string",
      "title": "Object key prefix",
      "description": "S3 prefix of the Triton model repository inside the bucket. Example: models/mkumatag/fraud"
    },
    "s3AccessKeyId": {
      "type": "string",
      "title": "S3 access key ID"
    },
    "s3SecretAccessKey": {
      "type": "string",
      "title": "S3 secret access key",
      "format": "password"
    },
    "modelName": {
      "type": "string",
      "title": "Model name",
      "description": "Name Triton will serve the model under. Must match the directory name downloaded from S3. Example: fraud_model"
    }
  }
}
