{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "type": "object",
  "additionalProperties": false,
  "required": ["model"],
  "properties": {
    "model": {
      "type": "object",
      "title": "Model source (S3)",
      "additionalProperties": false,
      "required": ["s3Endpoint", "s3Bucket", "s3Key", "s3AccessKeyId", "s3SecretAccessKey"],
      "properties": {
        "s3Endpoint": {
          "type": "string",
          "title": "S3 endpoint URL",
          "description": "Base URL of the S3-compatible object store. Example: https://s3.us-south.cloud-object-storage.appdomain.cloud"
        },
        "s3Bucket": {
          "type": "string",
          "title": "Bucket name",
          "description": "Name of the bucket that holds the model archive."
        },
        "s3Key": {
          "type": "string",
          "title": "Object key",
          "description": "Key of the model archive inside the bucket. Example: models/fraud_xgb_onnx_v1.tar.gz"
        },
        "s3AccessKeyId": {
          "type": "string",
          "title": "S3 access key ID"
        },
        "s3SecretAccessKey": {
          "type": "string",
          "title": "S3 secret access key",
          "format": "password"
        }
      }
    }
  }
}
